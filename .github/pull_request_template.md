## What and why

<!-- What does this change do, and what problem does it solve? -->

## Protocol impact

<!-- Does this touch the A2A (agent↔agent) path, the MCP (agent↔tool) path, or
     the provider/model layer? Note any behavior change. -->

## Checklist

- [ ] `pytest` passes locally (offline mock provider)
- [ ] `ruff check .` passes
- [ ] Added/updated tests for the change
- [ ] Updated docs (README / ARCHITECTURE / integrations) if behavior changed
- [ ] No secrets or private data added (config comes from env / `.env`)
